import React, { Component } from 'react';
import NavBar from './NavBar';

class Read extends Component {
    render() {
        return (
            <NavBar/>

        );
    }
}

export default Read;
